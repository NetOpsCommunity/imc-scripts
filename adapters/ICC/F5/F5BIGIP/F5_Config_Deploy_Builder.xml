<?xml version="1.0"?>
<definition>

	<action name="deploy_startup_config">
		<step>
    		<command name="deploy_startup_config_ftp" description="deploy to startup configuration via FTP through CLI"/>
		</step>
		<step>
			<command name="load_sys_ucs" description="load to the device running configuration"/>
		</step>
	</action>

    <action name="save_config">
		<step>
			<command name="save_config" description="save the device running configuration to startup"/>
		</step>
	</action>
	
	<action name="reload_device">
		<step>
			<command name="reload_device" description="reboot device"/>
		</step>
	</action>

</definition>
